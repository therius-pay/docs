---
title: "Coloca una reserva de fondos"
sidebarTitle: "Autorizar un pago"
description: "Reserva fondos sin capturarlos. Llama a capture más tarde para liquidar, o a cancel para liberar la retención. Ideal para bienes físicos."
openapi: "POST /payment/authorization"
translationOf: api-reference/authorize.mdx
translationSourceHash: d9e97944ee209fc6cd3d80249d4708d361336c7e64be623d6e39fc5dda2013da
---

import { SchemaLangNote } from "/snippets/schema-lang-note.mdx";

`POST /payment/authorization` reserva fondos en la cuenta del titular de la tarjeta sin liquidarlos. La retención de autorización suele ser válida durante 7 días, aunque la ventana exacta depende del adquirente y de la red de tarjetas. Llama a [Capture](/api-reference/capture) para liquidar los fondos cuando estés listo para cumplir, o a [Cancel](/api-reference/cancel) para liberar la retención si no puedes cumplir el pedido.

<SchemaLangNote lang="es" />

Consulta la referencia de [Purchase](/api-reference/purchase) para la descripción completa de los campos de `card`/`shopper`/respuesta — la forma de la solicitud y la respuesta es idéntica, solo cambia el resultado (una retención en vez de un cargo liquidado).

<RequestExample>

```bash cURL
curl -X POST https://api.therius.io/v1/payment/authorization \
  -H "Authorization: Bearer prv_production_your_key_here" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d '{
    "merchantCode": "MERCHANT_001",
    "orderCode": "ORDER-20240101-002",
    "amount": { "currency": "USD", "value": 9900, "exponent": 2 },
    "card": {
      "cardData": {
        "cardNumber": "4111111111111111",
        "cardholderName": "Ada Lovelace",
        "expiryMonth": "12",
        "expiryYear": "2030",
        "cvv": "123"
      }
    }
  }'
```

</RequestExample>

<ResponseExample>

```json 200 OK
{
  "id": "9f8b2c1e-4d5a-6b7c-8d9e-0f1a2b3c4d5e",
  "status": "authorized",
  "paymentCode": "PC-1234567890",
  "orderCode": "ORDER-20240101-002",
  "merchantCode": "MERCHANT_001",
  "amount": { "currency": "USD", "value": 9900, "exponent": 2 },
  "card": {
    "cardData": { "brand": "visa", "cardNumber": "411111****1111", "type": "credit" },
    "networkTransactionId": "txn_def456"
  }
}
```

</ResponseExample>

<Note>
  Se soporta la captura parcial. Cuando llamas a [Capture](/api-reference/capture), puedes pasar un `amount` menor o igual al monto autorizado. Por ejemplo, si autorizaste $$99.00 pero solo enviaste $$75.00 en bienes, captura \$75.00 y la retención restante se libera automáticamente.
</Note>

<Warning>
  Las retenciones de autorización expiran — normalmente después de 7 días, pero esto varía según el adquirente y la red de tarjetas. Si no llamas a [Capture](/api-reference/capture) antes del vencimiento, la retención se libera y necesitarás volver a autorizar. Monitorea la antigüedad de la retención en tu sistema de gestión de pedidos.
</Warning>
