---
title: "Cancela una suscripción"
sidebarTitle: "Cancelar una suscripción"
description: "Cancela una suscripción de inmediato. Dispara el evento de webhook subscription.cancelled. No se puede deshacer."
openapi: "POST /subscription/{id}/cancel"
translationOf: api-reference/subscriptions/cancel.mdx
translationSourceHash: 6cda64953e947635a32f6292564f8ca376bbb1fdbc091eeabee3555efc015ef2
---

import { SchemaLangNote } from "/snippets/schema-lang-note.mdx";

`POST /subscription/{id}/cancel` cancela una suscripción de inmediato. Esta acción dispara el evento de webhook `subscription.cancelled` y no se puede deshacer — la suscripción pasa a estado `cancelled` de forma permanente. Si quieres detener la facturación temporalmente sin perder la suscripción, usa [Pausar y reanudar](/api-reference/subscriptions/pause-resume) en su lugar.

<SchemaLangNote lang="es" />

<Warning>
  La cancelación es permanente. No hay endpoint de reinstauración. Si necesitas volver a inscribir a un cliente cancelado, crea una nueva suscripción usando `POST /subscription`.
</Warning>

Devuelve `200 OK` con el objeto de suscripción actualizado — `status: "cancelled"` y `cancelledAt` establecido.

## Errores

| Código | Significado |
| --- | --- |
| `400` | La suscripción ya está cancelada. |
| `404` | Suscripción no encontrada. |

<RequestExample>

```bash cURL
curl -X POST https://api.therius.io/v1/subscription/sub_abc123def456/cancel \
  -H "Authorization: Bearer prv_production_your_key_here" \
  -H "Content-Type: application/json" \
  -d '{ "merchantCode": "MERCHANT_001" }'
```

</RequestExample>

<ResponseExample>

```json 200 OK
{}
```

</ResponseExample>
